import { StyleSheet } from 'react-native';
export default styles = StyleSheet.create({
  cameraContainer: {
    width: '100%',
    height: '100%',
  },
  cameraUIView: {
    position: 'absolute',
  },
  menuButton: {
    borderWidth: 1,
    borderColor: 'black',
    alignItems: 'center',
    justifyContent: 'center',
    marginLeft: 10,
    marginTop: 10,
    width: 200,
    height: 100,
  },
});
